# Regenerate a Git Commands File

Since this file is ignored by git, you will have to regenerate it when
forking a repository. This function writes the template file to your R
directory as "regenerated_git_commands.R".

## Usage

``` r
regenerate_git_commands()
```

## Value

nothing
